apply plugin: "war"

bootWar {
    mainClass = "com.okta.demo.notification.NotificationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
